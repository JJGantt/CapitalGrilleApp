import Foundation

// Copy this file to `Secrets.swift` and paste your real Anthropic API key.
// `Secrets.swift` is gitignored.
enum Secrets {
    static let anthropicAPIKey = "sk-ant-REPLACE_ME"
    // The ANON key, never the service-role key: this ships to testers.
    static let supabaseKey = "eyJ-REPLACE_ME"
}
